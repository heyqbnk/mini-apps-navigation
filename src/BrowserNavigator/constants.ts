/**
 * Makes navigator replace current location
 * @type {string}
 */
export const REPLACE_MOD = 'replace';

/**
 * Makes navigator go to previous location
 * @type {string}
 */
export const BACK_MOD = 'back';

/**
 * Makes navigator skip location until location without this modifier is met
 * @type {string}
 */
export const SKIP_MOD = 'skip';
