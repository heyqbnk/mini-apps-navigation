/**
 * Makes navigator replace current state
 * @type {string}
 */
export const REPLACE_MOD = 'replace';

/**
 * Makes navigator go to previous state
 * @type {string}
 */
export const BACK_MOD = 'back';

/**
 * Makes navigator skip state until state without this modifier is met
 * @type {string}
 */
export const SKIP_MOD = 'skip';
